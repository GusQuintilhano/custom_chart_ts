{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../trellis-chart && npm install && npm run build && cd ../boxplot-chart && npm install && npm run build && cd ../charts-router && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd charts-router && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "watchPatterns": [
    "trellis-chart/src/**",
    "boxplot-chart/src/**",
    "charts-router/src/**",
    "shared/**"
  ]
}

