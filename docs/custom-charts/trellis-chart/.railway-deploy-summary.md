# Resumo: Arquivos para Deploy no Railway

> **Nota**: Este documento descreve o processo de deploy do Trellis Chart (anteriormente chart-07-multi-measures-sdk) no Railway.

## ✅ Arquivos que SERÃO enviados (apenas o necessário)

```
trellis-chart/
├── package.json          ✅ Dependências e scripts
├── tsconfig.json         ✅ Configuração TypeScript  
├── vite.config.ts        ✅ Configuração Vite
├── railway.json          ✅ Configuração Railway
└── src/                  ✅ Código fonte
    ├── index.html        ✅ HTML base
    ├── index.ts          ✅ Código principal
    └── test-local.ts     ✅ Código para testes locais
```

**Total: 7 arquivos essenciais**

## ❌ Arquivos EXCLUÍDOS (não enviados)

### Arquivos deste chart:
- 📄 `README.md` - Documentação
- 📄 `SETUP.md` - Guia de setup  
- 📄 `DEPLOY.md` - Guia de deploy
- 📄 `MIGRACAO_CHART_SDK.md` - Documentação técnica
- 📄 `RAILWAY_FILES.md` - Este arquivo
- 📄 `manifest.json` - Não necessário para Chart SDK
- 🔧 `build.sh` - Script local
- 📁 `node_modules/` - Instalado no Railway
- 📁 `dist/` - Gerado no build
- 📁 `.git/` - Não necessário

### Outros charts (não precisam ser enviados):
- 📁 `../chart-01-encodings/` - Chart 01 (não usa SDK)
- 📁 `../chart-02-layers/` - Chart 02 (não usa SDK)
- 📁 `../chart-03-transforms/` - Chart 03 (não usa SDK)
- 📁 `../chart-04-interactivity/` - Chart 04 (não usa SDK)
- 📁 `../chart-05-customization/` - Chart 05 (não usa SDK)
- 📁 `../chart-06-boxplot/` - Chart 06 Boxplot (não usa SDK)
- 📁 `../chart-06-multi-measures/` - Chart 06 Multi (não usa SDK)

## 🚀 Processo no Railway

1. **Upload:** Apenas os 8 arquivos acima
2. **Install:** `npm install` (instala dependências)
3. **Build:** `npm run build` (gera `dist/`)
4. **Start:** `npm start` (serve `dist/` na porta $PORT)

## 📊 Tamanho estimado

- **Código fonte:** ~5-10 KB
- **Configuração:** ~2 KB
- **Total enviado:** ~10-15 KB
- **Após build:** ~50-100 KB (com dependências)

## ✅ Checklist antes do deploy

- [ ] `package.json` tem script `start`
- [ ] `vite.config.ts` configurado
- [ ] `tsconfig.json` configurado
- [ ] `src/index.ts` existe
- [ ] `src/index.html` existe
- [ ] `.railwayignore` configurado (exclui outros charts)
- [ ] **Root Directory configurado no Railway:** `trellis-chart`
- [ ] Teste local: `npm run build` funciona
- [ ] Teste local: `npm start` funciona

## ⚠️ IMPORTANTE: Configuração do Root Directory

Ao conectar o repositório no Railway, **configure o Root Directory**:

```
Root Directory: trellis-chart
```

Isso garante que:
- ✅ Apenas o trellis-chart seja deployado
- ✅ Os outros charts (chart-01 a chart-06) sejam ignorados
- ✅ O build funcione corretamente

